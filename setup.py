# setup.py is for set up src for the modules

from setuptools import find_packages, setup

setup(
    name = 'Medical Chatbot',
    version= '0.0.0',
    packages= find_packages(),
    install_requires = []
)